// src/environments/environment.prod.ts
export const environment = {
  production: true,
  /** Absolute Police API base so both dev and prod call the same host */
  apiBase: 'https://data.police.uk/api',
};